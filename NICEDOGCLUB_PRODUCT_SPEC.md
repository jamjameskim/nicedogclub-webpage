# 우쥬산책 서비스 상세 기획안

> 문서 버전: v2.0
> 작성일: 2026-08-15 · 최종 갱신: 2026-09-04
> 대상: Product, UX/UI, iOS/Android, Backend, QA, 운영
> 기준 자료: v1.2(설계 초안) + 이후 실제 구현(Expo SDK 57 + Supabase, iOS 우선 출시) 전체 반영

> **이 버전에서 달라진 것** — v1.2는 개발 시작 전 설계 초안이었다. 실제로
> 만들면서 방향이 크게 바뀐 지점들:
> - 분실 알림·주변 목록의 "반경(1/3/5km) 선택"은 전부 폐기하고 **"같은
>   동" 기준**으로 통일했다(§5, §11.4, §14).
> - **동네친구(경로 매칭)·친한친구(모임)·하트(좋아요)** 소셜/게이미피케이션
>   기능이 통째로 새로 생겼다 — 탭 구조 자체가 바뀌었다(§4).
> - **앱 없이도 여는 게스트 목격 제보 웹페이지**(walk.wouldyou.me)를
>   콜드스타트 대응으로 추가했다(§13.5).
> - 알림은 즉시 트리거(좋아요·매칭·모임가입·제보·해결) + 리텐션
>   크론(하루 1회 다이제스트 + 개인화된 산책 리마인더) 이원 구조로
>   실제 구현됐다(§18).
> - §19 이후(데이터 모델·API·분석·운영자 도구)는 원안이 REST API +
>   전용 백엔드를 가정했지만, 실제로는 **Supabase(Postgres RLS +
>   SECURITY DEFINER 함수 + Edge Function + pg_cron)** 위에서 훨씬
>   가볍게 구현됐다 — 그 차이를 각 섹션에 명시했다.
> - 분석 이벤트 트래킹, 운영자 웹 콘솔은 **아직 미구현**이다(Supabase
>   Table Editor로 수동 운영 중) — 계획 문서가 아니라 실제 상태를
>   기준으로 표시했다.

## 1. 문서 목적

우쥬산책(Would You Walk)은 반려견과의 일상적인 산책을 기록하고, 공공데이터 기반 쓰레기통 위치를 지도에서 안내하며, 반려견 분실 시 마지막 위치와 같은 동네 사용자에게 신속히 알리고 목격 정보를 보호자에게 전달하는 지역 기반 서비스다. 여기에 산책으로 마주친 이웃과 이어지는 소셜 기능(동네친구·친한친구 모임·하트)을 더해, 위급할 때만 열게 되는 앱이 아니라 매일 열 이유를 만든다.

이 문서는 화면 구현에 필요한 값과 상태뿐 아니라 실제 서비스 운영에 필요한 위치·알림·개인정보·신뢰·신고·장애 대응 기준까지 정의한다. 별도 표시가 없는 수치와 정책은 실제 반영된 값이며, 코드 안의 상수(주로 각 기능 파일 상단 주석)가 최종 근거다 — 이 문서와 코드가 어긋나면 코드가 맞다.

---

## 2. 제품 목표와 원칙

### 2.1 목표

1. 사용자가 별도 학습 없이 2탭 이내로 산책을 시작하게 한다(홈 화면 FAB).
2. 산책 기록에서 생성되는 위치·사진·시설 정보를 일상적 효용으로 돌려준다.
3. 분실 신고 등록 후 곧바로 같은 동 이웃에게 알림 발송을 완료한다.
4. 목격자가 앱이 없어도 링크 하나로 위치·시각·사진을 보호자에게 전달하게 한다(콜드스타트 대응).
5. 신고자의 정확한 집 주소나 목격자의 현재 위치가 불필요하게 공개되지 않게 한다.
6. 산책이라는 행동 자체에 사회적 보상(하트·동네친구)을 붙여 위급 상황이 아니어도 앱을 열 이유를 만든다.

### 2.2 핵심 원칙

- **긴급 기능 우선:** 활성 분실 신고(내 신고 또는 진행 중인 산책)는 홈의 하트·날씨·통계보다 위에 노출한다.
- **입력보다 확인:** 반려견 프로필을 분실 신고서에 자동 채우고, 사용자는 확인·수정만 한다.
- **최소 공개:** 지도에는 근사 위치를 표시하고 정확 좌표는 권한과 목적에 따라 제한한다.
- **행동 중심 문구:** `제보하기`보다 `목격했어요`, `종료`보다 `찾기 완료`처럼 결과가 분명한 CTA를 사용한다.
- **안전 우선:** 목격자에게 접근·추격·포획을 요구하지 않고 거리 확보와 보호자 연락을 안내한다.
- **판단 가능한 정보 제공:** 쓰레기통은 공공데이터와 `없어졌어요` 누적 수만 제공하고, 이용 가능 여부는 사용자가 판단한다.
- **실패는 실패로 보여준다(추가된 원칙):** 위치를 못 잡았거나 데이터를 못 가져왔을 때, 그럴듯한 기본값으로 대체해 안심시키지 않는다. "성산동 좌표로 대체", "우리 동네는 조용해요(사실은 조회 실패)" 같은 처리를 전부 금지한다 — 실제로 이런 실수가 여러 번 있었고 매번 고쳤다.
- **관계가 걸린 쓰기는 클라이언트가 우회 못하게 한다(추가된 원칙):** 하트 주기, 모임 가입, 동네친구 매칭 기록처럼 "정말 그 관계인지" 확인이 필요한 쓰기는 테이블에 insert 정책을 아예 안 두고 SECURITY DEFINER 함수로만 허용한다.

### 2.3 실제 구현 범위 (v2.0 기준)

- 카카오/구글 로그인, 필수 약관 동의
- 반려견 1마리 등록 및 수정(계정당 1마리 전제로 확정 — 데이터 모델은 배열이라 확장 여지는 남김, §19 참고)
- 산책 시작·일시정지·재개·종료, 경로·시간·거리·사진 저장. 완료 화면에서 바로 제목·메모·사진 추가 가능(§9.4)
- 산책 중 공공데이터 기반 쓰레기통 지도 조회 및 `없어졌어요` 제보 수 확인
- 분실 신고 생성·게시·갱신·종료, 4단계(반려견 확인 → 목격 정보 → 자주 다니던 곳 노출 여부 → 게시)
- **동 기반** 분실 푸시 알림 및 인앱 상세(반경 방식 폐기)
- 목격 제보 생성(앱 사용자 + **비회원 웹 링크** 둘 다), 보호자 실시간 반영, 결과 알림 구독
- 사진 모아보기, 산책 기록 목록/캘린더, "자주 갔던 곳" 히트맵
- 주변 분실 지도(내 동네/전체보기 토글), 내 활동, 알림·공개 범위·차단 설정
- 콘텐츠 신고, 차단(양방향 RESTRICTIVE 정책으로 완전 비노출)
- **동네친구**(산책 경로가 겹친 이웃 자동 매칭) · **친한친구 모임**(초대코드)
- **하트(좋아요)** — 동네친구·모임 멤버 사진에 하루 1회, 산책 1km당 1개 자동 적립
- **리텐션 푸시** — 하루 1회 다이제스트(우선순위 6트리거) + 개인화된 산책 리마인더(본인 평소 산책 시각에 맞춰 발송)

### 2.4 후속 범위

- 실시간 채팅·전화번호 중계 통화
- 동물병원·보호소·경찰/지자체 연계
- AI 사진 유사도 검색
- 배지·리워드, 웨어러블 연동
- 쓰레기통 외 배변봉투함·급수대·반려견 출입 시설
- 분석 이벤트 트래킹, 운영자 웹 콘솔(현재는 Supabase 대시보드로 수동 운영)
- Android 출시(현재 iOS 우선)

---

## 3. 사용자와 권한

| 역할 | 설명 | 주요 권한 |
|---|---|---|
| 비회원 | 앱 설치 후 로그인 전 사용자 | 서비스 소개, 약관/개인정보 확인 |
| 비회원(웹) | 앱 없이 게스트 제보 링크로만 접근 | 목격 제보 텍스트 입력·전송만(walk.wouldyou.me/find) |
| 일반 회원 | 로그인 및 기본 권한 동의 완료 | 주변 신고 열람, 목격 제보, 쓰레기통 조회 및 `없어졌어요` 제출 |
| 보호자 | 반려견을 등록한 회원 | 산책 기록, 분실 신고 생성·관리·종료, 동네친구/모임 하트 |
| 제보자 | 활성 분실 건에 목격 정보를 보낸 회원 | 본인 제보 수정/철회, 결과 알림 수신 |
| 운영자 | CS/안전 운영 담당 | Supabase Table Editor로 신고 검수·콘텐츠 숨김·사용자 제재(전용 웹 콘솔 없음) |

한 계정은 보호자와 제보자 역할을 동시에 가질 수 있다.

---

## 4. 정보 구조와 전역 내비게이션

하단 탭은 다음 5개다(v1.2 초안의 `산책` 탭은 실제로는 없다 — 산책 시작은 홈 화면의 떠 있는 버튼이다).

| 탭 | 목적 | 대표 화면 |
|---|---|---|
| 홈 | 오늘의 행동 유도와 긴급 상황 노출 | 분실/진행중 산책 배너, 반려견+하트, 날씨, 캘린더, 이번 주 통계, 산책 시작 FAB |
| 주변 | 주변 분실 신고와 시설 지도 | 분실/쓰레기통 레이어, 내 동네·전체보기 토글, 상세 바텀시트 |
| 친구 | 동네친구·친한친구 모임 | 경로 매칭 이웃 목록(하트 포함), 모임 생성/참여, 멤버 주간 통계 |
| 기록 | 산책 기록과 사진 회고 | [산책 기록]·[사진] 서브탭 — 기록 목록만(캘린더는 홈/전체보기 화면이 맡음), 사진 모아보기 |
| 마이 | 프로필과 서비스 설정 | 반려견 관리, 알림 설정, 위치·공개 범위, 차단, 고객센터 |

산책 중에는 화면을 나가도 실제 GPS 기록은 계속된다(추적은 화면 컴포넌트가 아니라 별도 모듈이 들고 있음) — 탭 바 바로 위에 "산책 기록 중 · 거리 · 경과 시간" 바가 어느 탭에 있든 항상 뜨고, 누르면 산책 화면으로 돌아간다.

---

## 5. 전역 변수와 운영 설정값

하드코딩된 상수이며, 바꾸려면 코드 수정이 필요하다(Remote Config는 아직 없음).

| 변수 | 값 | 위치/용도 |
|---|---:|---|
| 분실 알림 대상 | **같은 동**(문자열 완전 일치) | 반경 방식 폐기(0024). 알림은 **지금 있는 곳**으로 매번 새로 역지오코딩해서 계산 |
| `walk.distanceInterval` | 2m | 백그라운드 GPS 최소 이동 간격 |
| `walk.timeInterval` | 1500ms | GPS 수집 주기 |
| `MAX_ACCURACY_M` | 25m | 이보다 부정확한 픽스는 버림 |
| `MAX_SPEED_MS` | 10m/s | 이 속도를 넘는 이동은 좌표 튐으로 간주해 버림 |
| 동네친구 매칭 반경 | 40m | 두 산책 경로가 이 이내로 겹치면 매칭 |
| 동네친구 매칭 시간창 | ±20분 | 두 산책이 이 시간 이내로 겹쳐야 매칭 |
| `METERS_PER_HEART` | 1000m | 하트 적립 — 걸은 거리 1km당 1개 |
| 하트(좋아요) 제한 | 1인 1일 1회 | 상대 1명당 하루 한 번(KST 자정 기준), 총량 제한 없음 |
| `daily-digest` 실행 시각 | 매일 09:00 KST | 우선순위 6트리거 중 하나만 발송 |
| `walk-reminder` 판정 주기 | 15분마다 | 개인화 리마인더 — 자세한 규칙은 §9.6 |
| `MIN_WALKS_FOR_AVERAGE` | 3건 | 이 미만이면 개인화 대신 09시 고정 |
| 축하 메시지 시각 | 22:00 KST | 오늘 산책했으면 이 시각에 한 번 |
| `notification_outbox` 중복 방지 창 | 20시간 | 같은 트리거를 이 시간 내 또 안 보냄 |
| `regionForPath` 위경도 델타 상한 | 60도 | 넘으면 지도 크래시 방지용 클램프 |
| 쓰레기통 신고 쿨다운 | 24시간 | 동일 시설 중복 제보 제한(유지) |

---

## 6. 공통 권한과 동의

### 6.1 권한 요청 시점

| 권한 | 요청 시점 | 거부 시 처리 |
|---|---|---|
| 알림 | 로그인 완료 후 | 인앱 알림만, 설정 유도 |
| 위치(사용 중) | 주변 지도 또는 산책 시작 최초 진입 | 수동 지도 위치 선택 허용 |
| 위치(항상) | 첫 산책 시작 시 | 거부돼도 산책은 되지만 화면을 꺼두면 기록이 끊길 수 있음을 명시 안내(전경 추적으로 자동 대체). 이후 설정에서 권한을 올리면 앱이 포그라운드로 돌아올 때마다 재확인해서 자동으로 백그라운드 추적으로 승격한다 |
| 카메라 | 산책 사진/목격 사진 촬영 시 | 앨범 선택 대안 제공 |
| 사진 | 이미지 선택 시 | 카메라 촬영 대안 제공 |

### 6.2 필수/선택 동의

- 필수: 이용약관, 개인정보 수집·이용, 만 14세 이상 확인
- 선택: `notify_marketing`(리텐션·소셜 알림 전체를 아우르는 단일 토글) — 분실/목격 같은 `safety` 카테고리 알림은 이 토글과 무관하게 항상 나간다.

---

## 7. 온보딩 및 반려견 등록

### 7.1 로그인

- 로그인 수단: 카카오, 구글(Apple 로그인 미구현).
- 최초 로그인 → 약관 동의 → 반려견 등록.
- 재로그인 시 서버에 백업된 반려견·산책·사진을 복구한다(기기 변경 대응). 이 복구가 끝나기 전에 "반려견 없음 = 신규"로 잘못 판단해 재등록을 유도하는 버그가 있었고, 세션 재수립 재시도 로직으로 고쳤다.

### 7.2 반려견 필드

실제 구현은 v1.2 초안보다 단순하다 — `microchip_registered`, `neutered`, `temperament` 세분화 등은 만들지 않았다.

| 필드 | 필수 | 비고 |
|---|---:|---|
| 대표 사진 | Y | `user-photos` 버킷(비공개), 매칭된 동네친구까지만 열람 가능 |
| 이름 | Y | |
| 견종 | Y | |
| 털 색(`coatColors`) | Y | 최대 3개 |
| 생년월 | N | 나이로 변환해 표시 |
| 성별 | Y | |
| 몸무게 | N | |
| 사람을 경계함(`waryOfPeople`) | N | 분실 신고 시 경고 배너로 노출 |

계정당 반려견은 사실상 1마리 전제다(화면은 전부 `usePet()` 단일 선택만 사용). 데이터는 배열로 저장해 향후 다견 확장 여지를 남겼지만, 중복 등록은 버그로 취급해 자동 정리한다(`dedupePets`).

---

## 8. 홈

### 8.1 노출 우선순위 (실제 구현)

1. 인사(날짜·이름) + 분실 신고 버튼(사이렌) + 알림함
2. 진행 중인 산책 배너(있으면)
3. **내 분실 신고 현황**(내가 낸 활성 신고 — 위치·동과 무관하게 항상 표시) 또는 우리 동네 분실 배너
4. 반려견 사진·이름 + **하트**(받은 좋아요 + 산책 거리 기반 합산, 알약 탭 시 적립 규칙 설명 팝업)
5. 최근 추억(사진 가로 스크롤)
6. 산책 날씨
7. **캘린더**("산책기록" 라벨, 그 달 산책을 달력으로, "전체보기"로 상세 대시보드 이동)
8. 이번 주 통계(요일별 별, 산책 횟수·거리·시간)
9. 산책 시작/이어보기 — 화면 하단에 항상 떠 있는 FAB

"최근 추억"·"이번 주"·"산책 시작"이 본문 중간에 나열돼 있던 v1.2식 배치는 폐기했다 — 성격이 다른 섹션이 한 화면에 쌓이면 우선순위가 안 읽힌다는 판단으로, 동네친구 미리보기·최근 산책 목록은 아예 빼고(각각 친구 탭·기록 탭에 이미 있음) 산책 시작만 항상 접근 가능한 FAB로 뗐다.

### 8.2 하트 배너

반려견 이름 옆 하트 알약을 누르면 "친구들이 하트를 눌러주거나, 산책을 하다보면 하트가 쌓여요" 팝업이 뜬다. 자세한 규칙은 §9.7.

### 8.3 산책 날씨

- 표시값: 날씨 요약, 기온, 좋음/나쁨 판단(맑음 계열 코드 기준).
- 위치 미동의/API 실패 시 `현재 날씨를 불러올 수 없어요`만 표시하고 산책은 막지 않는다.

---

## 9. 산책 기록

### 9.1 시작 전 / 권한

- 반려견 미등록이면 시작 자체를 막고 등록 화면으로 유도한다.
- 위치 권한이 "사용 중에만"이면 화면 상단에 경고 배너("화면을 잠그면 경로가 끊길 수 있어요")를 띄운다. 설정에서 "항상 허용"으로 올리면 앱이 다시 포그라운드로 돌아올 때 자동 감지해 그 자리에서 백그라운드 추적으로 승격한다.

### 9.2 산책 중 화면

- 상단 고정 지표: 경과 시간, 거리.
- 지도: 현재 위치, 이동 경로.
- 하단 원형 버튼: 일시정지/재개, 종료, 카메라.
- 쓰레기통 위치 보기 토글.
- **화면을 나가도 추적은 계속된다** — 뒤로가기/스와이프로 다른 탭에 갈 수 있고, 탭 바 위 진행 상황 바가 항상 떠 있다(§4).

### 9.3 거리 및 경로 계산

1. 정확도 25m 초과, 속도 10m/s 초과(비현실적 이동) 픽스는 버린다.
2. 3m 미만 이동은 GPS 지터로 보고 좌표를 안 쌓는다.
3. 유효 좌표 간 haversine 거리를 누적한다.
4. 일시정지 구간은 거리·시간에서 제외한다.
5. 위경도 델타가 60도를 넘는 경로는 지도 렌더링 시 클램프한다(네이티브 지도 크래시 방지 — 실제로 서울↔해외를 오간 경로에서 `MKMapView`가 죽은 적이 있다).

### 9.4 종료

- 종료 → `endWalk()` → 요약 화면(`walk/summary.tsx`).
- 요약 화면에서 **바로 제목·메모·사진을 추가**할 수 있다(v2.0 신규 — 나중에 기록 상세에서만 가능하던 것을 완료 시점으로 당김, 그 순간의 기억이 흐려지기 전에 남기게 하려는 목적).
- 저장("즐거웠어요")을 누르면 저장 전/후 총 거리를 비교해 이번 산책으로 실제로 늘어난 하트 개수를 계산하고, 1개 이상이면 하트가 튀어 오르는 애니메이션을 보여준 뒤 마일스톤 축하/동네친구 매칭 안내로 이어간다.
- 경로 길이가 2점 미만이면(위치를 못 잡고 바로 종료) 저장하지 않는다는 걸 명확히 안내한다.

### 9.5 진행 중 산책의 생존성

앱이 완전히 종료됐다가(iOS가 메모리 회수 등으로 프로세스를 죽인 경우) 위치 이벤트로 headless 재기동될 때, 원래는 진행 중이던 산책 정보가 메모리에만 있어서 통째로 사라지는 문제가 있었다. 지금은:

- 진행 중인 산책 상태(id·시작시각·경로·거리)를 별도 경량 로컬 키에 즉시 스냅샷으로 남긴다(메인 상태 저장소에는 의도적으로 안 남김 — 반쪽짜리 산책을 잘못 이어 붙이는 것보다, 앱을 사용자가 직접 종료한 경우엔 재시작이 낫다는 판단).
- headless 재기동 시 이 스냅샷으로 복원한 뒤 좌표를 이어 붙인다.
- 죽어있던 구간 자체의 이동은 원천적으로 복구 불가능하다(위치 이벤트를 아예 못 받았으므로) — 이건 "잃지 않는" 안전망이지 "메워주는" 게 아니다.

### 9.6 산책 리마인더 (신규, 개인화 푸시)

산책 안 한 날, 사람마다 평소 걷는 시간이 달라서 하루 한 시각으로 통일하지 않는다.

- 최근 산책이 3건 이상이면 **본인 최근 산책 시작 시각의 평균**(KST)에 맞춰, 그 시각에 오늘 산책이 아직 없으면 그 순간 발송한다. 버퍼(예: +1~2시간)를 일부러 안 둔다 — 늦게 보낼수록 "이미 걸었는데 기록만 깜빡한" 사람을 잘못 찌를 확률이 올라간다.
- 3건 미만(신규)이면 09시 고정.
- 지금 산책이 진행 중이면(막 시작한 경우 포함) 리마인더를 스킵한다 — 앱이 그 순간 서버에 "산책 중" 신호를 보내둔다. 앱이 죽어 종료 신호를 못 보낸 경우를 대비해 4시간 넘은 신호는 무시한다.
- 오늘 산책을 이미 했으면 22시에 "오늘도 산책 완료 ❤️" 축하 메시지를 한 번 보낸다.
- 판정은 15분 주기 크론이 돈다(`walk-reminder` Edge Function).

### 9.7 하트(좋아요)

- 총 하트 = 동네친구·모임 멤버에게 받은 좋아요(서버 저장) + 걸은 총 거리 1km당 1개(저장 없이 매번 계산).
- 좋아요는 친구 탭에서 상대 사진을 확대했을 때 하트 버튼으로 준다. 하루에 같은 상대에게 한 번만(KST 자정 기준), 총량 제한은 없음.
- 자기 자신에게는 못 준다. 관계 확인(동네친구 또는 같은 모임 멤버)은 서버 함수 안에서만 하고, 클라이언트가 우회할 수 없다.
- 좋아요를 받으면 그 자리에서 상대에게 즉시 푸시가 간다(`"{이름}가(이) 하트를 눌렀어요!"`).
- 동네친구·모임 목록에 뜨는 하트 숫자는 홈 화면과 같은 계산식을 쓴다(받은 좋아요 + 그 사람 산책 거리 기반) — 처음엔 "받은 좋아요"만 세다가 실제 수치와 안 맞아 보여 지금 방식으로 바꿨다.

---

## 10. 쓰레기통 지도

v1.2 초안과 대부분 동일하게 구현됐다.

### 10.1 지도 표현

- `주변` 지도와 산책 중 오버레이에서 쓰레기통 레이어를 토글한다.
- 공공데이터 기반 위치만 표시하며 사용자가 추가·수정할 수 없다.
- `없어졌어요` 제보가 누적돼도 핀을 자동으로 숨기지 않는다.

### 10.2 시설 상세 / `없어졌어요` 제보

- 거리, 유형, 주소, 출처를 표시하고 값이 없으면 `정보 없음`.
- 유일한 CTA는 `없어졌어요`, 동일 시설 24시간 1회 제한, 10초 실행취소 제공.

---

## 11. 분실 신고 — 보호자 플로우

### 11.1 진입

- 홈의 사이렌 버튼, 분실 배너에서 진입. 이미 활성 신고가 있으면(위치와 무관하게 항상 감지) 새 신고서 대신 그 화면으로 보내 중복 신고를 막는다.
- 반려견 미등록이면 등록 유도.

### 11.2 1단계: 반려견 확인

등록된 프로필에서 이름·사진·견종·성별·나이·체중·특징을 자동 채운다.

### 11.3 2단계: 마지막 목격 정보

위치(지도 확정 시 동네 이름도 함께 역지오코딩), 시각(TimePicker — 오늘/어제 + 시/분 5분 단위 휠, 방금/과거 프리셋), 사람을 경계하는지 여부를 받는다.

### 11.4 3단계: 자주 다니던 곳 노출 (신규 단계)

- 이 신고자의 산책 경로를 약 11m 격자로 뭉친 빈도표를 보여줄지 선택한다(기본값 **켬** — 이 앱의 다른 공개성 토글과 다르게 유일하게 기본 켬으로 간 지점, 급한 상황에 무심코 놓치기 쉬운 토글을 꺼둘 이유가 없다는 판단).
- 켰으면 목격자 화면과 보호자 타임라인 화면에 "자주 다니던 곳" 히트맵으로 노출된다.
- 찾기 완료 시 이 데이터는 서버에서 실제로 삭제된다("신고 끝나면 안 보여요" 안내를 실제로 지킴).

**v1.2의 "알림 반경(1/3/5km) 선택"은 없다.** 신고 시점에 있던 곳의 "동"으로 자동 결정되고, 예상 도달 인원 표시는 "이 동에 사는 활성 사용자 수" 기준으로 바뀌었다.

### 11.5 수신 대상 계산

- **같은 동**(last_seen_neighborhood와 profiles.neighborhood 문자열 완전 일치)인 사용자 전원.
- 상대 프로필의 동 값은 **그 사람이 최근 앱을 열었을 때 있던 곳**이다(집 동네 고정값이 아니다) — 알림을 받을 확률보다 "받았을 때 실제로 찾아볼 수 있는지"를 우선했다.
- 차단 관계면 어느 방향으로도 노출되지 않는다(RESTRICTIVE 정책).

### 11.6 진행 중 사건 관리

- 헤더에 경과 시간, 제보 수. 지도에는 마지막 목격 지점(**검정 핀**)과 제보 순서(**주황 번호 핀**) — 색을 다르게 줘서 같은 지점 근처일 때도 구분되게 했다(원래는 둘 다 주황이라 헷갈렸음).
- 헤더 아래 "링크 복사" 띠배너 — walk.wouldyou.me 게스트 제보 링크를 다른 채널(당근마켓 등)에 직접 올릴 수 있게.
- 뒤로가기·스와이프·하드웨어 백 전부 막혀 있다 — "찾는 중" 상태에서 무심코 나가면 서버 상태와 화면이 어긋나기 때문에, 나가는 길은 "홈으로 이동" 하나로 통일했다.
- **주소가 이제 텍스트로도 표시된다**(지도만으로는 정확한 지번이 안 읽혀서 추가).

### 11.7 종료

- `찾기 완료` → 상태 `resolved`. "자주 다니던 곳" 데이터를 서버에서 삭제.
- 제보자 전원에게 결과 알림(`notify_on_resolved` 트리거).

### 11.8 사건 상태

`searching → resolved`. v1.2의 `EXPIRED`/`CANCELLED`/`SUSPENDED` 세분화는 아직 구현하지 않았다(수동 운영으로 대체).

---

## 12. 분실 알림 — 주변 사용자 경험

### 12.1 푸시 구성

- 새 분실 신고 → **같은 동** 사용자에게 발송(반경 아님, §11.5).
- 알림함 배지는 "지금 주변에 신고가 있나"가 아니라 "아직 안 본 신고가 있나" 기준으로 켜진다 — 알림함을 한 번 열면, 그 신고가 여전히 활성 상태라도 배지가 다시 안 켜진다.

### 12.2 상세 화면 (목격자용, `find/[caseId]`)

- 사진, 찾는 중 상태, 견종/성별/나이 요약.
- **마지막 목격 위치를 지도 + 텍스트 둘 다**로 표시.
- 사람을 경계하는 아이면 경고 배너.
- "자주 다니던 곳" 히트맵(신고자가 노출을 켰을 때만).
- 하단 CTA: `목격했어요`. 우측 상단: `신고`(콘텐츠 신고, 대상 신고).

---

## 13. 목격 제보 플로우

### 13.1 입력 (앱 사용자)

위치(드래그 가능한 지도), 시각(방금/15분전/1시간전/직접), 사진, 메모를 받는다.

### 13.2 전달 완료

새 제보는 사건 타임라인·지도에 실시간 반영되고 보호자에게 즉시 푸시가 간다(`notify_on_sighting` 트리거).

### 13.3 앱 제보 vs 게스트 제보 구분

`sightings.is_guest`로 구분한다. 보호자 타임라인에서 게스트 제보에는 "?" 아이콘이 붙고, 누르면 "외부에서 신고된 건이에요 — 제보자가 앱 사용자가 아니라 정확한 위치 대신 글로만 남겼어요" 설명이 뜬다.

### 13.4 제보 상태

`pending → confirmed`(보호자가 "확인함" 처리). 철회 UI는 아직 없음.

### 13.5 비회원 게스트 제보 (신규 — 콜드스타트 대응)

- `walk.wouldyou.me/find?case=<id>` — 별도 저장소(Vercel)에서 서빙하는 순수 웹페이지. 앱 설치 없이 아무나 열어서 제보할 수 있다.
- **위치는 텍스트(자유 입력)만 받는다.** 브라우저 GPS로 자동 채우는 걸 검토했다가 명시적으로 폐기했다 — 제보자가 "지금 이 폼을 쓰는 위치"가 "목격한 위치"와 다를 수 있어서(걷다 본 뒤 집에 가서 작성하는 경우 등), 틀릴 수 있는 좌표보다 정직한 텍스트가 낫다는 판단. 좌표는 신고의 마지막 목격 좌표를 중립적인 자리로만 쓴다.
- 사진이 있으면 카카오톡/당근마켓 공유 시 `og:image`로 미리보기 카드가 뜨게 처리.
- 신고 상태가 `resolved`면 "이미 찾았어요" 안내로 대체하고 폼을 안 보여준다.
- 원래 Supabase Edge Function으로 만들었으나, 플랫폼이 Edge Function의 `text/html` 응답을 `text/plain`으로 강제 변환하는 제약이 있어(Pro+커스텀 도메인 없이는 우회 불가) Vercel로 옮겼다.

---

## 14. 주변 지도

### 14.1 레이어와 필터

- 레이어: `분실`, `쓰레기통`(배타 선택).
- 분실 레이어에 **"내 동네"/"전체보기" 토글**(신규) — 기본은 지금 있는 동 기준(알림 대상과 동일 기준), 전체보기는 동 제한 없이 "찾는 중"인 사례 전체(최대 200건)를 보여준다. 사례가 실제로 얼마나 많은지 가리지 않기 위해 추가했다.
- 쓰레기통 레이어는 기존대로 3km 반경.
- 현재 위치는 파란 점 하나로만 표시, 다른 사용자 위치는 어떤 형태로도 표시하지 않는다.

### 14.2 분실 핀

- 주황색 핀. 바텀시트: 모드에 따라 "{동 이름} {count}건" 또는 "전체 {count}건".
- 동을 못 알아냈으면(역지오코딩 실패) 빈 목록 대신 "동네를 확인하지 못했어요, 전체보기로 볼 수 있어요" 안내로 정직하게 처리한다.

### 14.3 위치 공개

- 목격 제보의 정확 좌표는 일반 공개 지도에 안 뿌린다. 앱 제보는 지도 핀 + 타임라인, 게스트 제보는 타임라인 텍스트만.

---

## 15. 기록과 사진

### 15.1 기록 탭 (구조 변경)

`기록` 탭 안에 [산책 기록]·[사진] 서브탭이 있다(v1.2의 "기록/사진" 세그먼트 아이디어와 방향은 같으나 실제 UI는 최상위 서브탭 전환 방식).

- **산책 기록**: 목록만(캘린더는 아래 §15.2가 별도로 맡음).
- **사진**: 3열 그리드, 산책과 연결된 사진은 그 산책 통계와 함께 확대.

### 15.2 캘린더 / 전체보기

- 홈 화면의 "캘린더" 섹션 → "전체보기"를 누르면 월 캘린더 + 통계 + "자주 갔던 곳" 히트맵(전체 기간 기준)을 보여주는 대시보드 화면으로 이동한다.

### 15.3 사진

- 삭제해도 연결된 산책 기록은 유지한다.
- 스토리지에 파일이 실제로는 없는(삭제됐거나 업로드 실패한) "유령 사진"이 빈 자리로 남는 문제가 있었다 — 이미지 로드가 실제로 실패하는 순간에만 존재를 재확인해 지우도록 고쳤다(서명 URL 발급 자체는 파일 존재를 보장하지 않기 때문).

---

## 16. 마이(내 정보와 설정)

- 프로필, 반려견 관리, 로그아웃/탈퇴.
- 신고·차단 관리, 알림 설정(`notify_marketing` 단일 토글).
- 고객센터, 약관, 공유하기.

---

## 17. 콘텐츠 신고와 차단

v1.2 초안대로 구현됐다.

### 17.1 신고 대상 / 사유

분실 사건, 목격 제보, 사용자. 허위 정보·도용·괴롭힘·사기 의심·개인정보 노출·기타.

### 17.2 처리

- `reports` 테이블은 insert-only — 클라이언트는 조회할 수 없고 운영자만 Supabase Table Editor로 확인한다.
- `blocked_users` — 차단하면 `lost_cases`·`sightings`에 RESTRICTIVE 정책이 걸려 **어떤 조회 경로로도** 서로 안 보인다(양방향).

---

## 18. 알림 정책 (실제 구현 — 이원 구조)

v1.2가 그렸던 단일 알림 테이블 대신, 실제로는 **즉시 트리거**와 **크론 기반 리텐션**으로 나뉘어 있고 둘 다 같은 큐(`notification_outbox`)와 발송기(`flush-outbox` Edge Function)를 공유한다.

### 18.1 즉시 트리거 (쓰기 시점에 바로 큐잉 → 클라이언트가 flush-outbox 호출)

| 이벤트 | 수신자 | category | trigger_key |
|---|---|---|---|
| 목격 제보 등록 | 신고 주인 | safety | `sighting` |
| 분실 해결 | 제보자 전원 | safety | `resolved` |
| 모임 가입(신규 멤버만, 재입장 제외) | 기존 멤버 전원 | social | `group_join` |
| 동네친구 매칭(이번에 처음 만난 경우만) | 상대방 | social | `neighbor_match` |
| 하트(좋아요) — 오늘 처음 준 경우만 | 받은 사람 | social | `pet_like` |
| 새 분실 신고 | 같은 동 사용자 전원 | safety | (broadcast-lost) |

`safety` 카테고리는 `notify_marketing`이 꺼져 있어도 항상 나간다. 나머지는 꺼져 있으면 큐에 아예 안 쌓인다.

### 18.2 리텐션 크론

| 함수 | 주기 | 내용 |
|---|---|---|
| `daily-digest` | 매일 09:00 KST | 우선순위대로 딱 하나만: ①어제 걸었는데 오늘 아직(`streak_warning`) ②날씨 좋고 최근 3일 산책 0건(`weather_nudge`) ③가입 24~48h 반려견 미등록(`signup_no_pet`) ④반려견 등록 48h+ 산책 0건(`pet_no_walk_48h`) ⑤3~4일 미접속(`inactive_3d`) ⑥7~8일 미접속(`inactive_7d`). 하루 여러 알림은 스팸으로 느껴진다는 원칙으로 딱 하나만 고른다 |
| `walk-reminder` | 15분마다 | §9.6 참고 — 본인 평소 산책 시각에 맞춘 개인화 리마인더 또는 22시 축하 메시지 |

### 18.3 조용한 시간 / 발송 상태

v1.2가 제안한 "조용한 시간(22~08시)" 정책은 아직 별도로 구현하지 않았다(단, `walk-reminder`의 09시/평균시각 기준 발송이 사실상 이른 아침·심야를 피하는 효과는 있음). 발송 상태 컬럼(`sent_at`, `error`)은 있으나 `QUEUED/SENT/DELIVERED/OPENED` 세분화된 상태 머신은 없다.

---

## 19. 데이터 모델 (실제 스키마 요약)

Postgres(Supabase) + RLS. 전체 정의는 `supabase/migrations/*.sql`(마이그레이션 번호 순, 0001~0035+)이 원본이다.

| 테이블 | 주요 컬럼 | 비고 |
|---|---|---|
| `profiles` | id, name, neighborhood, home_lat/lng, push_token, notify_marketing, matching_enabled, active_walk_started_at | `neighborhood`는 "최근 앱을 연 곳" 기준(§11.5) |
| `pets` | owner_id, name, breed, coat_colors, photo_url, is_primary | |
| `walks` | owner_id, client_id, title, note, started_at, ended_at, distance_m, path(jsonb) | 백업 시스템이 로컬 기록을 여기 동기화(§9.5의 서버 존재 근거) |
| `photos` | owner_id, walk_id, storage_path | |
| `lost_cases` | owner_id, pet_name, last_seen_lat/lng, last_seen_neighborhood, status, route_distribution(jsonb) | `radius_km` 컬럼은 삭제됨(0024) |
| `sightings` | case_id, reporter_id(nullable), is_guest, location, at, verification | reporter_id nullable = 게스트 제보 허용(0027) |
| `neighbor_encounters` | user_a, user_b, first_matched_at, last_matched_at, match_count | 동네친구 매칭 기록 |
| `friend_groups` / `friend_group_members` | invite_code 등 | 친한친구 모임 |
| `pet_likes` | liker_id, target_id, liked_on(date) | `unique(liker_id, target_id, liked_on)`로 하루 1회 강제 |
| `reports` / `blocked_users` | | insert-only / 당사자만 조회 |
| `notification_outbox` | user_id, category, trigger_key, title, body, data, sent_at | §18의 공용 큐 |

### 19.2 위치 저장 원칙

- 산책 경로 원본은 서버에 저장되지만 본인만 RLS로 조회 가능. 집계값(거리 합계 등)만 SECURITY DEFINER 함수를 통해 관계 있는 상대(모임 멤버·동네친구)에게 노출한다 — 원본 행은 절대 안 내보낸다.
- 프로필 위치는 소수점 2자리(약 1km 격자)로 뭉개서 저장한다.
- 목격 정확 좌표의 자동 삭제(TTL)는 아직 구현하지 않았다 — 신고가 `resolved`될 때 `route_distribution`만 명시적으로 지운다.

---

## 20. 백엔드 아키텍처 (v1.2의 "API 초안" 대체)

v1.2는 전용 REST API 서버를 가정했지만, 실제로는 **Supabase 하나로** 구현했다.

- **읽기/쓰기 대부분**: PostgREST(Supabase 자동 생성 REST) + RLS 정책으로 직접.
- **관계 확인이 필요한 쓰기**(하트 주기, 모임 가입, 동네친구 매칭 기록 등): 클라이언트가 테이블을 직접 못 쓰게 막고(insert 정책 없음), `SECURITY DEFINER` Postgres 함수(RPC)로만 허용. 예: `like_neighbor()`, `join_group_by_code()`, `find_walk_neighbors()`.
- **크론 작업**: `pg_cron` + `pg_net`으로 Edge Function을 주기 호출(`daily-digest`, `walk-reminder`). 인증은 사용자 JWT가 아니라 Vault에 저장한 전용 시크릿(`x-cron-secret` 헤더)으로 한다.
- **알림 발송**: `notification_outbox`에 쌓고 `flush-outbox` Edge Function이 Expo Push API로 실제 발송.
- **비회원 웹 제보**: 별도 정적 사이트(Vercel) + Edge Function이 Supabase REST를 서비스 키로 직접 호출.

이 구조를 고칠 때 반드시 지켜야 하는 규칙(자주 걸렸던 함정 포함)은 `docs/app-state.md`(Nicedogclub 저장소)에 별도로 정리돼 있다 — PL/pgSQL의 `RETURNS TABLE` 컬럼명 함정, 마이그레이션 이력을 끝까지 확인해야 하는 이유 등.

---

## 21. (v1.2의 "요청/응답 변수 예시" — RPC 방식으로 대체)

REST 바디 예시 대신, 실제로는 Supabase RPC를 이런 식으로 호출한다.

```ts
// 하트 주기
await supabase.rpc('like_neighbor', { p_target_id: targetUserId });

// 분실 신고 게시(테이블 insert, RLS: 본인만)
await supabase.from('lost_cases').insert({ owner_id, pet_name, last_seen_lat, ... });
```

파라미터 이름·타입의 최종 근거는 각 RPC를 정의한 마이그레이션 파일이다.

---

## 22. 비기능 요구사항

원안의 목표는 유효하나, 부하·침투 테스트 등은 사용자 규모가 아직 작아 실측하지 않았다.

### 22.1 성능 / 22.2 안정성 (실제 대응한 것 위주)

- 산책 중 프로세스가 죽어도(iOS 메모리 회수) headless 재기동 시 진행 중 상태를 복원한다(§9.5).
- 좌표 튐·저정확도 픽스 필터링(§5).
- 지도 렌더링 시 극단적으로 벌어진 좌표(위경도 델타 60도 초과)를 클램프해 네이티브 크래시를 막는다.

### 22.3 보안

- 소셜 로그인 토큰은 SecureStore(Keychain)에 저장, 나머지 상태와 분리.
- 관계가 걸린 쓰기는 전부 SECURITY DEFINER 함수 경유(§20).
- 로그에 정확 좌표·토큰 원문을 남기지 않는다.

### 22.4 접근성

목표는 v1.2와 동일(WCAG AA, 44×44pt 터치 영역 등)하나 별도 접근성 감사는 진행하지 않았다.

---

## 23. 예외 및 경계 조건 (실제로 겪고 고친 것 위주)

| 상황 | 처리 |
|---|---|
| 위치 못 잡음 | 기본 좌표로 대체하지 않고 실패 상태를 그대로 보여준다(§2.2 원칙) |
| 서울↔해외처럼 좌표가 크게 벌어진 경로 | `regionForPath()` 델타 클램프로 지도 크래시 방지 |
| 스토리지에 파일 없는 사진(서명 URL은 발급됐지만) | 로드 실패 시점에 존재 재확인 후 삭제 |
| 로그인 직후 Supabase 세션 지연 | 복구 시도를 한 번 더 재시도한 뒤 판단(신규 오인 방지) |
| 산책이 진행 중인데 앱이 죽음 | 스냅샷으로 복원(§9.5), 리마인더는 서버 신호로 스킵(§9.6) |
| 신고한 곳과 지금 있는 곳의 동이 다름 | "내 신고 현황"은 위치·동과 무관하게 별도 조회로 항상 표시 |
| 동을 못 알아냄(역지오코딩 실패) | 빈 목록 대신 실패 안내 + 전체보기 유도 |
| 이미 활성 분실 신고 있음 | 새 신고서 대신 기존 사건으로 이동(중복 신고 방지) |

---

## 24. 분석 이벤트와 KPI

**미구현.** 이벤트 트래킹 SDK(Amplitude/Mixpanel 등)를 아직 붙이지 않았다. v1.2가 정의한 이벤트 목록(§24.1 원안 참고)은 향후 붙일 때의 설계 초안으로는 유효하다.

---

## 25. 운영자 도구

**전용 웹 콘솔 없음.** 신고 검수, 콘텐츠 숨김, 사용자 제재는 Supabase 대시보드(Table Editor, SQL Editor)로 수동 처리한다. 사용자 규모가 커지면 §25 원안 수준의 도구가 필요하다.

---

## 26. QA 인수 기준

v1.2의 항목 대부분 유효하다. 실제 구현 과정에서 추가된 인수 기준:

- 위치 권한을 산책 중 "사용 중에만"에서 "항상 허용"으로 올리면, 앱을 다시 열었을 때 안내 배너가 사라지고 실제로 백그라운드 추적이 시작된다.
- 신고한 위치와 다른 동에 있어도 홈 화면의 "내 분실 신고 현황"이 뜬다.
- 동네친구/모임 목록에서 하트 버튼을 누르면 채워진 채로 유지되고(화면을 나갔다 들어와도), 이미 오늘 준 상대는 다시 눌러도 "이미 보냈어요"로만 안내한다.
- 산책 중 화면을 나가도(다른 탭 이동) GPS 기록이 끊기지 않는다.

---

## 27. 개발 우선순위와 출시 단계 (갱신)

### 완료 (v1.2의 Phase 0~2에 대응)

인증, 반려견 등록, 산책 기록(오프라인 복구 포함), 쓰레기통 지도, 분실 신고 3→4단계, 동 기반 알림, 목격 제보(앱+게스트), 콘텐츠 신고/차단.

### 완료 (v1.2에 없던 것)

동네친구·모임·하트, 개인화 산책 리마인더, 비회원 게스트 제보 웹페이지, 백그라운드 산책 유실 방지.

### 다음

- 사용자 규모 확보(무료 채널 기반 로컬 비즈니스 파트너십, §별도 GTM 문서 참고)
- 최소 하나의 유료 기능/상품으로 수익화 가설 검증
- Android 출시
- 분석 이벤트 트래킹 도입

---

## 28. 출시 전 반드시 확정할 정책

v1.2와 동일하게 유효 — 아직 사업/법무 확정이 필요한 항목이다.

1. 서비스 지원 국가·지역(현재 한국어·iOS 우선, 사실상 한국 전용 가정)
2. 정확 위치·산책 경로·사진·신고 제보의 보존·삭제 기간(현재 명시적 TTL 없음)
3. 보호자·제보자 간 연락 방식(현재 앱 내 전달만)
4. 허위 신고·악성 제보 제재 단계
5. 미성년자 가입 기준
6. 경찰·소방·지자체·보호소 안내 문구 및 면책 표현
7. 공공 쓰레기통 데이터 이용 조건·갱신 주기
8. 개인정보 사고 대응 담당자·통지 절차

---

## 29. 화면 문구 변수 규칙

v1.2와 동일한 원칙 유지 — 시간(`40분 전`), 거리(`850m`/`1.2km`), 행정구역(`동` 수준)은 동적 변수로 렌더링하고 하드코딩하지 않는다.

---

## 30. 최종 사용자 플로우 요약 (갱신)

### 평상시

`로그인 → 반려견 등록 → 홈 → 산책 시작(FAB) → 경로/사진 기록 → 종료 시 제목·메모·사진 바로 추가 → 하트 획득 애니메이션 → 기록/사진 회고`

### 동네친구·하트 (신규)

`산책 → 경로가 겹친 이웃과 자동 매칭 → 친구 탭에서 확인 → 사진 확대 후 하트 → 상대에게 즉시 푸시 → 하루 지나면 다시 줄 수 있음`

### 분실 시 보호자

`사이렌 버튼(활성 신고 있으면 그 화면으로) → 반려견 확인 → 마지막 목격 위치·시각 입력 → 자주 다니던 곳 노출 여부 선택 → 게시(같은 동 자동 계산) → 링크 복사로 외부 공유 가능 → 제보 타임라인 확인 → 찾기 완료`

### 주변 사용자

`같은 동 푸시 수신 → 분실 상세(주소 텍스트 포함)·안전 안내 확인 → 목격했어요 → 위치(텍스트)·시각·사진 입력 → 보호자에게 전달`

### 비회원(앱 없음, 신규)

`카톡/당근마켓 등에서 공유된 링크 클릭 → walk.wouldyou.me/find → 반려견 정보 확인 → 목격 위치(텍스트)·시각·메모 입력 → 전송(앱 설치 불필요)`

### 쓰레기통 정보

`주변/산책 지도 → 공공데이터 쓰레기통 레이어 → 핀 위치 확인 → 출처·기준일·없어졌어요 누적 수 확인 → 현장에 없으면 없어졌어요 제출`
